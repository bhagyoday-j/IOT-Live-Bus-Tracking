{
  "name": "smarttransit-device-simulator",
  "version": "1.0.0",
  "description": "Simulates the SmartTransit in-bus IoT device over MQTT (no hardware needed). Publishes GPS location + sensor telemetry exactly as the ESP32 firmware does.",
  "main": "device-simulator.js",
  "scripts": {
    "start": "node device-simulator.js"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "mqtt": "^5.10.0"
  }
}
